# axon_bbs/requirements.txt
# Core Django and dependencies
Django==5.0.6
djangorestframework==3.15.0  # Latest stable as of July 2025
djangorestframework-simplejwt==5.3.1
python-dotenv==1.0.1  # For .env file support
django-cors-headers==4.4.0  # Latest

# Cryptography for encryption/signing
cryptography==43.0.0  # Latest stable

# BitTorrent support (Python bindings for libtorrent; may require system install)
# Note: python-libtorrent is not always available via pip; install system-wide first:
#   Ubuntu/Debian: sudo apt install python3-libtorrent libtorrent-rasterbar-dev
#   Or build from source: pip install git+https://github.com/arvidn/libtorrent.git
# Fallback to latest available version if manual install is preferred
# python-libtorrent==2.0.10  # Commented out due to unavailability; use system package

# Requests for HTTP over Tor
requests==2.32.3

# Optional: For production/testing
gunicorn==22.0.0  # Updated
psycopg2-binary==2.9.9  # If switching to PostgreSQL

# Ensure compatibility with Python 3.11/3.13
